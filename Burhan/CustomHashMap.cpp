
// Thin compilation unit: CustomHashMap is header-only (template implementations
// live in CustomHashMap.h). This file exists so the project keeps a .cpp for
// the class without duplicating template definitions or changing functionality.

#include "CustomHashMap.h"
using namespace std;
