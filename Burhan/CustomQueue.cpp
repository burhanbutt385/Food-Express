using namespace std;
// CustomQueue.cpp – intentionally left empty because the full implementation is provided via the header include. This prevents duplicate symbol errors when the file is compiled separately.
